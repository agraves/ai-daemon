[package]
name = "ai-run"
description = "Run a program with inference and nothing else: a narrowed session, and no network."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
libc.workspace = true
